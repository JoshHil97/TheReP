{% render 'rp-page-render', page_kind: 'how-it-works', page: page, section: section, title: page.title | default: 'How it works', show_breadcrumbs: true %}

{% schema %}
{
  "name": "RP Page how it works",
  "presets": [
    {
      "name": "RP Page how it works"
    }
  ]
}
{% endschema %}
