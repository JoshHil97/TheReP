{% render 'rp-page-render', page_kind: 'sizing', page: page, section: section, title: page.title | default: 'Sizing', show_breadcrumbs: true %}

{% schema %}
{
  "name": "RP Page sizing",
  "presets": [
    {
      "name": "RP Page sizing"
    }
  ]
}
{% endschema %}
