{% render 'rp-page-render', page_kind: 'delivery', page: page, section: section, title: page.title | default: 'Delivery & returns', show_breadcrumbs: true %}

{% schema %}
{
  "name": "RP Page delivery",
  "presets": [
    {
      "name": "RP Page delivery"
    }
  ]
}
{% endschema %}
